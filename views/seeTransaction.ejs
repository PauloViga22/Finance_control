<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="style.css" type="text/css" />
  <title>See</title>
</head>

<body style="	margin: 0;
padding: 0;
border: 0;
font-size: 100%;
font: inherit;
vertical-align: baseline;">
  <div class="seeIt" style="  width: 100vw;
  height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background-color: black;
  color: white;
  font-size: 1.5em; ">
    <% console.log(transaction) %>
    <h1> <%= transaction.title %> </h1>
    <h3> <%= transaction.date %> </h3>
    <p> <%= transaction.amount %> </p>
    <p> <%= transaction.type %> </p>

    <br>

    <% if(editMode) { %>
    <form action='/updateTransaction' method='POST'>
      <input style="  font-size: 1.8em;
      margin-bottom: 0.2em;
      border-radius: 0.3em;
          padding: 0.2em;
  color: white;
  background-color: black;" type="hidden" name="transactionId" value='<%=transaction._id %>'>
      <input style="  font-size: 1.8em;
      margin-bottom: 0.2em;
      border-radius: 0.3em;
          padding: 0.2em;
  color: white;
  background-color: black;" type='text' name='title' placeholder="Title: " value='<%=transaction.title %>'>
      <br>
      <input style="  font-size: 2.12em;
      margin-bottom: 0.2em;
      border-radius: 0.3em;
          padding: 0.2em;
  color: white;
  background-color: black;" type='datetime-local' name='date' placeholder="Title: " value='<%=transaction.date %>'>
      <br>
      <input style="  font-size: 1.8em;
      margin-bottom: 0.2em;
      border-radius: 0.3em;
          padding: 0.2em;
  color: white;
  background-color: black;" type='number' name='amount' placeholder="Amount: " value='<%=transaction.amount %>'
        min="0.00" step="0.01">
      <br>

      <div class="test" style="    position: relative;
      left: 45px;">
        <input type="radio" name="type" value="income" style="height: 3em; width: 4em;">
        <p style="display: contents;
      font-size: 2em;">income</p>
        <input type="radio" name="type" value="expense" checked style="height: 3em; width: 4em;">
        <p style="display: contents;
      font-size: 2em;">expense</p>
      </div>
      <br>
      <button style="      font-size: 2em;
    background-color: rgb(59, 147, 255);
    border-radius: 0.3em;
        padding: 0.2em;
    color: white;
    border-width: 0.1em;
    position: relative;
      left: 186px;
      top: 4px;   " type='submit'>Send</button>
    </form>
    <%  }  %>
  </div>

</body>

</html>